# Mapbox public token used by the frontend only (map rendering).
# Use a public token (pk.) with URL/origin restrictions configured in Mapbox.
MAPBOX_TOKEN=pk.your-public-mapbox-token

# =============================================================================
# Geo Services Configuration
# =============================================================================
# When running with Docker Compose, use internal service names:
#   - Nominatim: http://nominatim:8080
#   - Valhalla: http://valhalla:8002
# When running outside Docker or connecting to external services, use IP/hostname.

# Valhalla - Routing Engine
# Internal Docker URL: http://valhalla:8002
VALHALLA_BASE_URL=http://valhalla:8002
VALHALLA_STATUS_URL=http://valhalla:8002/status
VALHALLA_ROUTE_URL=http://valhalla:8002/route
VALHALLA_TRACE_ROUTE_URL=http://valhalla:8002/trace_route
VALHALLA_TRACE_ATTRIBUTES_URL=http://valhalla:8002/trace_attributes

# Nominatim - Geocoding Service
# Internal Docker URL: http://nominatim:8080
NOMINATIM_BASE_URL=http://nominatim:8080
NOMINATIM_SEARCH_URL=http://nominatim:8080/search
NOMINATIM_REVERSE_URL=http://nominatim:8080/reverse
NOMINATIM_USER_AGENT=EveryStreet/1.0

# =============================================================================
# OSM Data Configuration
# =============================================================================
# Path to OSM data inside the container (managed by the app)
OSM_DATA_PATH=/osm/region.osm.pbf

# Geofabrik mirror for downloading OSM extracts (optional, defaults to main server)
GEOFABRIK_MIRROR=https://download.geofabrik.de

# =============================================================================
# Database Configuration
# =============================================================================
# MongoDB (when using Docker Compose, use service name)
MONGO_URI=mongodb://mongo:27017
MONGODB_DATABASE=every_street

# Redis (when using Docker Compose, use service name)
REDIS_URL=redis://redis:6379
